export default function Navbar({className}: {className: string}) {
    return <div className={`w-full bg-secondary text-secondary-foreground flex justify-center items-center p-4 ${className}`}>
        <h1>Dear dear,</h1>
    </div>;
}
